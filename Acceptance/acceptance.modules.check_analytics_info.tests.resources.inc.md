```cmake
RESOURCE_FILES(
    // Specifies a list of resource files that are part of the project or test data.

    data/bad_stable_responses/input_table.json
    // Path to the input table JSON file for test cases with bad stable responses.

    data/bad_stable_responses/megamind_analytics_info_checker.json
    // Path to the JSON file containing results from the `megamind_analytics_info_checker` for bad stable responses.

    data/bad_stable_responses/meta_checker.json
    // Path to the JSON file containing results from the `meta_checker` for bad stable responses.

    data/bad_stable_responses/tunneller_checker.json
    // Path to the JSON file containing results from the `tunneller_checker` for bad stable responses.

    data/bad_test_responses/input_table.json
    // Path to the input table JSON file for test cases with bad test responses.

    data/bad_test_responses/megamind_analytics_info_checker.json
    // Path to the JSON file containing results from the `megamind_analytics_info_checker` for bad test responses.

    data/bad_test_responses/meta_checker.json
    // Path to the JSON file containing results from the `meta_checker` for bad test responses.

    data/bad_test_responses/tunneller_checker.json
    // Path to the JSON file containing results from the `tunneller_checker` for bad test responses.

    data/good_responses/input_table.json
    // Path to the input table JSON file for test cases with good responses.

    data/good_responses/megamind_analytics_info_checker.json
    // Path to the JSON file containing results from the `megamind_analytics_info_checker` for good responses.

    data/good_responses/meta_checker.json
    // Path to the JSON file containing results from the `meta_checker` for good responses.

    data/good_responses/tunneller_checker.json
    // Path to the JSON file containing results from the `tunneller_checker` for good responses.
)
```
