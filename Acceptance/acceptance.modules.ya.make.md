```cmake
# Recursively include the specified directories in the build process.
RECURSE (
    # Include the 'check_analytics_info' directory, which likely contains code for validating analytics data.
    check_analytics_info
    # Include the 'render_div_cards' directory, which likely contains code for rendering DIV cards (e.g., for UI components).
    render_div_cards
    # Include the 'request_generator' directory, which likely contains code for generating requests (e.g., for testing or data processing).
    request_generator
)
```
